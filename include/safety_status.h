#ifndef SAFETY_STATUS_H
#define SAFETY_STATUS_H

// Define the SafetyStatus structure
struct SafetyStatus {
    bool isSafe;               // Indicates if the system is deemed safe
    bool isEmergencyActivated; // Indicates if emergency protocols are activated
    // Add more safety status fields as needed
};

#endif // SAFETY_STATUS_H
