#ifndef FILTERED_DATA_H
#define FILTERED_DATA_H

// Define the FilteredData structure
struct FilteredData {
    float filteredValue; // Filtered value after processing
    // Add more filtered data fields as needed
};

#endif // FILTERED_DATA_H
